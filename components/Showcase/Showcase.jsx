import style from './Showcase.module.css';

export const Showcase = ({ evt }) => {
	return (
		<div className={style.showcase}>
			<h1>Welcome To The Party!</h1>
			<h2>Find the hottest DJ events</h2>
		</div>
	);
};
